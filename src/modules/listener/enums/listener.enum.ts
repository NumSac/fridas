// Supporting enums
export enum Protocol {
    TCP = 'tcp',
    HTTP = 'http',
    HTTPS = 'https',
    WEBSOCKET = 'websocket',
}

// src/listeners/enums/status.enum.ts
export enum ListenerStatus {
    ACTIVE = 'active',
    INACTIVE = 'inactive',
}